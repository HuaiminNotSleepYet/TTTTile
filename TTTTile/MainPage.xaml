<Page
    x:Name="_page"
    x:Class="TTTTile.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TTTTile"
    xmlns:control="using:TTTTile.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Image x:Name="_backgroundImage" Stretch="UniformToFill"
               HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <ScrollViewer Margin="5" HorizontalAlignment="Center" VerticalAlignment="Stretch">
            <control:ImageTileView x:Name="_imageTileView"
                                   VerticalAlignment="Top"/>
        </ScrollViewer>

        <StackPanel Spacing="4" Margin="4" >
            <Button x:Name="_buttonSetBackgroundImage" Content="Set Background Image" Click="SetBackgroundImageAsync"/>
            <Button x:Name="_buttonSetTileImage" Content="Set Tile Image" Click="SetTileImageAsync"/>
        </StackPanel>
        
        <StackPanel Spacing="4" Orientation="Horizontal" 
                    Margin="4" HorizontalAlignment="Left" VerticalAlignment="Bottom">
            <Button x:Name="_buttonAddSmallTile" Content="Add Tile (Small)"/>
            <Button x:Name="_buttonAddMediumTile" Content="Add Tile (Medium)"/>
            <Button x:Name="_buttonAddWideTile" Content="Add Tile (Wide)"/>

        </StackPanel>

        <Button x:Name="_buttonPin" Content="Pin!"
                Margin="4" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
    </Grid>
</Page>
